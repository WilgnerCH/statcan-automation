name: Monthly StatCan Extraction

on:
  schedule:
    - cron: '0 10 16 * *'  # todo dia 16 às 10h UTC
  workflow_dispatch:       # permite rodar manual também

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install requests pandas python-dateutil

      - name: Run script
        run: python statcan_script.py

      - name: Commit CSV
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add *.csv || echo "No CSV files to add"
          git commit -m "Monthly data update" || echo "Nothing to commit"
          git push || echo "Nothing to push"
